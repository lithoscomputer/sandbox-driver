bin.name = "lithos-sandbox"
args = [
  "--provider",
  "host",
  "sandbox",
  "run",
  "--env",
  "BROKEN",
  "--",
  "/bin/true",
]
status.code = 1
stdout = ""
stderr = """
error: --env must use KEY=VALUE syntax
"""

[env]
inherit = false

[env.add]
NO_COLOR = "1"
PATH = "/usr/bin:/bin"
RUST_LOG = "off"
XDG_CONFIG_HOME = "tests/cmd/no-config"
